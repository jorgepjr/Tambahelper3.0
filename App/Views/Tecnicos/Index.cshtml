@model IEnumerable<Tecnico>
  @{
  Layout = "_Layout";
  ViewData["Title"] = "Lista de Técnicos";
  }

  @* <div class="form-group mt-1 mb-0 p-2">
    <a class="btn btn-link btn-sm" asp-controller="Helps" asp-action="Index"><i class="fas fa-arrow-left"></i>
      Voltar</a>

  </div> *@
    <div class="mt-3" style="margin-left: 94%; position: static; display: inline-block;">
      <a class="btn btn-info btn-sm" asp-controller="Tecnicos" asp-action="Criar"><i class="fas fa-plus"></i> Novo</a>
    </div>
  <div class="card card-body mt-2">
    <h3 class="card-title"><em class="fas fa-headset"></em> @ViewData["Title"]</h3>
      @if(@Model.Any()){
    <ul class=" list-group">
      @foreach(var item in Model){
      <a asp-action="Editar" asp-route-id="@item.Id">
        <li class="list-group-item list-group-item-action">
          @item.Nome
        </li>
      </a>
      }
    </ul>
    }else{
  <h3 class="card card-body alert-primary">Nenhum técnico cadastrado!</h3>
    }
  </div>