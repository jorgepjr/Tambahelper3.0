@model UsuarioViewModel
@{
ViewData["Title"] = "Criar Usuário";
}

<div class="card-body">
  <div class="card-title">
    <h3><i class="fas fa-user-plus"></i> @ViewData["Title"]</h3>
  </div>
  <form asp-action="Criar">
    <div class="row">
      <div class="form-group col-md-4">
        <select asp-for="TecnicoId" class="form-control" asp-items='@(new SelectList(@ViewBag.Tecnicos, "Id", "Nome"))'>
          <option value="">Selecione</option>
        </select>
        <span class="text-danger" asp-validation-for="TecnicoId"></span>
      </div>

      <div class="form-group col-md-4">
        <select asp-for="Perfil" class="form-control">
          <option value="">Selecione ...</option>
          <option value="Administrador">Administrador</option>
          <option value="Tecnico">Tecnico</option>
        </select>
        <span class="text-danger" asp-validation-for="TecnicoId"></span>
      </div>
      @* <div class="form-group col-md-2">
        <input asp-for="Perfil" class="form-control" placeholder="Perfil" />
        <span class="text-danger" asp-validation-for="Perfil"></span>
      </div> *@
      <div class="form-group col-md-3">
        <input asp-for="Email" class="form-control" placeholder="Email" />
        <span class="text-danger" asp-validation-for="Email"></span>
      </div>
      <div class="form-group col-md-3">
        <button class="btn btn-success">Salvar</button>
      </div>
  </form>
</div>